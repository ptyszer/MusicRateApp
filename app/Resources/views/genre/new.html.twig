{% extends 'base.html.twig' %}

{% block title %}Nowy gatunek muzyczny - MusicRate{% endblock %}
{% block body %}
    <h1>Genre creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" class="btn btn-primary" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('genre_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
