{% extends 'base.html.twig' %}

{% block title %}Wykonawcy - MusicRate{% endblock %}
{% block container %}
    <h4>Wykonawcy</h4>

    <table class="table">
        <thead>
        <tr>
            <th scope="col">Nazwa</th>
        </tr>
        </thead>
        <tbody>
        {% for artist in artists %}
            <tr>
                <td>
                    <a href="{{ path('artist_show', { 'id': artist.id }) }}">
                        {{ artist.name }}
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artist_new') }}">Dodaj nowego wykonawcę</a>
        </li>
    </ul>
{% endblock %}
