{% extends 'base.html.twig' %}

{% block title %}Nowy album - MusicRate{% endblock %}
{% block body %}
    <h4>Nowy album</h4>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" class="btn btn-primary" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('album_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
